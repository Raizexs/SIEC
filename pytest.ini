[pytest]
# Run only backend tests by default to avoid collection errors in mixed environments
testpaths = backend/tests
python_files = test_*.py
addopts = -q
norecursedirs = .git venv env node_modules frontend
